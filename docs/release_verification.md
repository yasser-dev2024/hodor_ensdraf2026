# تحقق إصدار 1.1.0+4

تاريخ التحقق: 2026-08-19 — Windows / Android 16 على جهاز حقيقي.

- `flutter analyze --no-pub`: لا أخطاء.
- `flutter test --no-pub`: 25 اختبارًا ناجحًا.
- اختبار الضغط: استيراد 5000 طالب بأسماء عربية طويلة، إغلاق قاعدة البيانات وإعادة فتحها، بقاء QR والحضور، ترحيل 5000 طالب مع 5000 سجل نقل، تعطيلهم دفعة واحدة دون محو السجلات، ثم `PRAGMA integrity_check = ok`.
- اختبار المعاملة: خريطة ترحيل ناقصة تتراجع بالكامل دون نقل جزئي.
- اختبار التقارير: أفضل طالب، الأكثر غيابًا، الأكثر استئذانًا، أفضل فصل، وأكثر فصل غيابًا بنتائج محددة مسبقًا.
- اختبار التاريخ: تقرير الصف السابق يبقى صحيحًا بعد الترحيل إلى صف جديد.
- اختبار النسخ: إنشاء نسخة مشفرة، استعادتها مع مفتاح البيانات، ورفض كلمة مرور خاطئة دون تغيير قاعدة البيانات الحالية.
- المصادقة المبسطة: اسم مستخدم وكلمة مرور فقط؛ لا ينتظر النموذج فحص البصمة أو Android Keystore، وتبقى الحسابات القديمة قابلة للدخول من حقل كلمة المرور.
- فحص Android الفعلي: شاشة إعداد المدير تعرض حقلين فقط، الكتابة في الاسم نجحت، وAndroid أبلغ `mInputShown=true` و`mIsInputViewShown=true`.
- بناء Release موقّع ومصغّر: ناجح.
- APK: `releases/morning-attendance-v1.1.0.apk`.
- الحجم: 92.2 MB (96,704,698 بايت).
- SHA-256: `8EF72A2D7339A62765D6A6DCE02DF545196AC8930E8088C5B674EF925576AB52`.
- توقيع APK Signature Scheme v2: صحيح، موقّع واحد، مفتاح RSA 4096.
- أذونات APK الفعلية: CAMERA وBIOMETRIC فقط مع أذونات Android الداخلية؛ لا INTERNET ولا ACCESS_NETWORK_STATE.
- النطاق المشبوه الذي حظره Avast غير موجود في المصدر أو APK.
- التثبيت باستخدام `adb install -r`: ناجح، والإصدار المثبت `1.1.0 (4)`.
- تشغيل Cold Start وشاشة الإعداد: ناجحان دون شاشة فارغة.
- سجل الجهاز: لا FATAL EXCEPTION ولا E/flutter ولا LocaleDataException ولا SQLiteException.
- `index.html`: الرابط والحجم وSHA-256 مطابقون للحزمة النهائية.
- التحقق المنشور: صفحة GitHub Pages ورابط زر Android أعادا `200 OK`، واكتمل تنزيل 96,704,698 بايت من الرابط نفسه ببصمة `8EF72A2D7339A62765D6A6DCE02DF545196AC8930E8088C5B674EF925576AB52`.
- GitHub: رُفع commit `f70a35e` إلى فرع `main` بنجاح.

## اعتمادات خارجية لا يمكن تنفيذها من Windows وحده

1. بناء وتوقيع iOS ورفع TestFlight يحتاج macOS وحساب Apple الخاص بالمدرسة.
2. القبول الميداني للطابعة الفعلية والبصمة الشخصية ومسح بطاقات الطلاب الحقيقية يحتاج دخول المستخدم وملحقاته.
